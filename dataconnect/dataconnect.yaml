specVersion: "v1"
serviceId: "drp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "drp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
